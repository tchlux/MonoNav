#include<stdio.h>
#include<stdlib.h>
int main()
{
  printf("\nHello World,\nWelcome to my first C program in Ubuntu Linux\n\n");
return(0);
}
//So I typed up all of this stuff exactly how the instructions said
//to, then I ran "gcc -Wall -W -Werror main.c -o HelloWorldC" from the
//command prompt.  gcc is the compiler, the flags tell the compiler to
//check for warnings and the -o states the output file (executable)
