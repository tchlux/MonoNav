#ifndef SYNTAX_CHECK

#define SYNTAX_CHECK

// Pre:  each char* in line is terminated, len >= 0
// Post: bool -> valid syntactic line
bool checkR (char ** line, int len){
  bool valid = true;
};

// Pre:  each char* in line is terminated, len >= 0
// Post: bool -> valid syntactic line
bool checkI (char ** line, int len){
  bool valid = true;
};

// Pre:  each char* in line is terminated, len >= 0
// Post: bool -> valid syntactic line
bool checkJ (char ** line, int len){
  bool valid = true;
};

// Pre:  each char* in line is terminated, len >= 0
// Post: bool -> valid syntactic line
bool checkO (char ** line, int len){
  bool valid = true;
};

// Pre:  each char* in line is terminated, len >= 0
// Post: bool -> valid syntactic line
bool checkU (char ** line, int len){
  bool valid = true;
};

#endif
