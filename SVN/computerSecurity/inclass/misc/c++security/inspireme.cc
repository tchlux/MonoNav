#include <iostream>  // for user input and program output
#define MAX_STRING_SIZE 30 //the array size of all words

using namespace std;

//Function:  This will enter the loop of prompting the user for words
//           and or connections to the current word, then it will save
//           the connections provided and move on.
//Pre:       The user enters valid, non-numerical strings
int main(int argc, char * argv[]){
  char response[MAX_STRING_SIZE]; //initialize response storage
}

void prompt_user(char * response){
  cin >> response
}
