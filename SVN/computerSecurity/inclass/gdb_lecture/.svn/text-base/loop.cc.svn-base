#include <iostream>
#include <cstdio>
#include <ctype.h>
using namespace std;

int main(int argc, char* argv[]){
  char c;
  cin >> c;
  while(c != EOF){
    if(isalnum(c))
      cout << c;
    else
      cin >> c;
  }

  return 1;
}
