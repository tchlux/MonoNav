
#pong
import bouncing_ball
import window

w = window.Window()

#set the balls velocity to be northeast
b = bouncing_ball.Ball(heading=NORTHEAST, speed=SPEED)

#set the ball at 100 pixels west of the right wall
b.set_x_coord(bouncing_ball.WINDOW_WIDTH // 2 - 100
b.set_y_coord(0)



#Expected behavior - the ball will be facing northeast (45 degrees) and moving at a rate of SPEED pixels per frame, it should bounce off the wall at a 45 degree angle facing NORTHWEST


