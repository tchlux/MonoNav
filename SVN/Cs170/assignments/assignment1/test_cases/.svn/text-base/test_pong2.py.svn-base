


#pong
import bouncing_ball
import bricks
import window

#the bricks above and below the ball
w = window.Window()
b = bouncing_ball.Ball(x=0, y=0, heading=NORTH, speed=SPEED)
w1 = brick.Brick(strength=2, x=0, y=50, color="red")
w2 = brick.Brick(strength=1, x=0, y=-50, color="blue")

#Expected behavior - the ball will bounce off the top brick, then the change to go towards the bottom brick, bounce of the bottom brick and break it, then bounce again of the top brick and break it


