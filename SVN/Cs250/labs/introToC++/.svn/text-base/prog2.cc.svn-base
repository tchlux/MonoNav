#include <iostream>
#include <string>    // This makes available the system string library.

using namespace std;

// PRE: 
// POST: Output stream (OS) contains the string "Hello world".
int main (int argc, char * argv[]) {

  string prompt = "Hello world"; // declare a string object called
				 // prompt and initialize it to a
				 // value. 
  cout << prompt << endl;
  // ASSERT: OS contains the value of prompt.

  return (0);
}
