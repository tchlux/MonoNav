#include <iostream>
#include "Pair.h"

using namespace std;

int main(int argc, char * argv[]){
  int first = 1;
  float second = 1.1;
  Pair<int, float> testPair(first, second); 
  Pair<float, int> testPair2(second, first);
  int sFirst = testPair.getFirst();
  float sSecond = testPair.getSecond();
  cout << "The pair == ( " << sFirst << " , "
       << sSecond << " )\n";

  return(0);
}
