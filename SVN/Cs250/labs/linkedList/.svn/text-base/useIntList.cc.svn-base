#include "intList.h"

int main () {

  intList myList;
  for (int i = 0; i < 10; i+=3) {
    myList.addFront (i);
  }
  // ASSERT: myList contains the items added.

  /* COMMENT 2
  intList myNewList = myList;   // myNewList is a copy of myList. Is
                                //  it a deep copy or a 
                                //  shallow copy?  
  cout << "Made new list" << endl;
  cout << "The size of the new list = " << myNewList.getSize() << endl;
  cout << myNewList << endl;
  cout << "Done printing myNewList." << endl;
     END COMMENT 2 */

  cout << "The size of the list = " << myList.getSize() << endl;
  cout << myList << endl;
  cout << "Done printing myList." << endl;


  cout << "Printing myList again" << endl;
  cout << myList << endl;
  cout << "Done printing myList a second time." << endl;


  return (0);
}
