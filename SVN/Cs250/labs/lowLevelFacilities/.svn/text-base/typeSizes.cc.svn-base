#include <iostream>
#include "String.h"

using namespace std;

// Pre:  Optional command line args
// Post: Size of various types are printed
int main(int argc, char * argv[]){
  int returnStatus = 0; //int variable for run condition

  // int integer; //ex. integer
  // unsigned int uInt; //ex. unsigned int
  // float cloud; //ex. floating point num
  // double tennis; //ex. double type
  // char pokemon; //ex. character type
  // long mile; //ex. long type
  // long long marathon; //ex. long long type
  // cout << "This is the size of program!  Check it out!!\n";
  // cout << "Integer           :" << sizeof(integer) << endl;
  // cout << "Unsigned Integer  :" << sizeof(uInt) << endl;
  // cout << "Float             :" << sizeof(cloud) << endl;
  // cout << "Double            :" << sizeof(tennis) << endl;
  // cout << "Character         :" << sizeof(pokemon) << endl;
  // cout << "Long              :" << sizeof(mile) << endl;
  // cout << "Long Long         :" << sizeof(marathon) << "\n\n";

  // int * aList; //ex. pointer
  // aList = new int[10]; //ex. filling pointer
  // cout << "Int *             :" << sizeof(aList) << endl;

  // char * char_list; //ex. pointer
  // char_list = new char[10]; //ex. filling pointer
  // cout << "Char *            :" << sizeof(char_list) << "\n\n";
  // delete (char_list);

  // String sentence; //ex. user defined String class
  // cout << "String            :" << sizeof(sentence) << endl;

  // int * newList; //ex. for printing
  // cout << "List     :" << newList << endl;
  // newList = new int[10];
  // cout << "List     :" << newList << endl;
  // newList = new int[10];
  // cout << "List     :" << newList << "\n\n";
  // delete (newList);

  // //for number 6
  // int x;
  // int * list;
  // list = new int[10];
  // cout << "Address of x is " << &x << endl;
  // cout << "Address of list is " << &list 
  //      << " and list contains " << list << endl;

  //Number 7
  int * x;
  int y;
  x = &y;
  y = 10;
  cout << "x contains " << x << endl;
  cout << "x points to the value " << (*x) << endl;
  cout << "y contains " << y << endl;
  cout << "y is at address " << &y << endl;
  (*x) = 15;
  cout << "\nx contains " << x << endl;
  cout << "x points to the value " << *x << endl;
  cout << "y contains " << y << endl;
  cout << "y is at address " << &y << endl;


  return (returnStatus); //normal execution
}
